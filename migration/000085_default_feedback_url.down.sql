-- 000085 は backfill のみで schema 変更を伴わないため no-op。
--
-- **意図的に巻き戻さない** (000084 と同じ)。up の実行後に operator が同じ値を明示設定した
-- 可能性があり、「元が未設定だった行」と「あとから同じ値を設定した行」を区別できない。
-- TS へ戻す場合は admin 画面で設定し直すこと。
SELECT 1;
