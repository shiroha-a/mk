-- 000067 の down は **意図的に no-op**。
--
-- up は migrations テーブルの name を TypeORM 形式へ書き換えるが、その逆変換
-- (正式名 → 短縮形) を無条件に流すと、Misskey TS が自分で書いた行まで壊して
-- しまう。mk-go 由来の行と TS 由来の行は適用後には区別できないため、安全に
-- 巻き戻す手段が無い。
--
-- 巻き戻しても得られるのは「TS 復帰時に全 migration が再実行される」という
-- 壊れた状態でしかないので、rollback しない方が常に安全。
SELECT 1;
