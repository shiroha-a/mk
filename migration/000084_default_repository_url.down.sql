-- 000084 は backfill のみで schema 変更を伴わないため no-op。
--
-- **意図的に巻き戻さない。** up の実行後に operator が同じ値を明示設定した可能性が
-- あり、「元が未設定だった行」と「あとから同じ値を設定した行」を区別できない。
-- 戻した先はソースコードの案内が消えた状態 (AGPL 13 条の観点で up 前より悪い) なので、
-- 区別できない以上そのままにする。TS へ戻す場合は admin 画面で設定し直すこと。
SELECT 1;
