-- 000068 の down は **意図的に no-op**。
--
-- up が落とすのは「upstream と定義が同一の重複 index」だけで、落とした後も
-- 同じ定義の upstream 由来 index が必ず残っている。したがって再作成しても
-- 二重化した状態に戻すだけで、得られるものが無い。
--
-- mk-go 由来 DB では up 自体が何も落とさないので、こちらでも復元対象は無い。
SELECT 1;
